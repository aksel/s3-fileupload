{
  "name": "s3-fileupload",
  "version": "1.0",
  "private": true,
  "scripts": {
    "start": "cross-env NODE_ENV=development BUCKET={YOUR_BUCKET} ACCESS_KEY_ID={YOUR_ACCESS_KEY} SECRET_ACCESS_KEY={YOUR_SECRET_ACCESS_KEY} node ./src/server.js"
  },
  "dependencies": {
    "aws-sdk": "^2.77.0",
    "express": "4.15.2",
    "multer": "^1.3.0",
    "node-uuid": "^1.4.8"
  },
  "devDependencies": {
    "cross-env": "3.1.3"
  }
}
